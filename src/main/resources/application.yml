spring:
  datasource:
    url: jdbc:mysql://localhost:3307/taskapp
    username: root
    password: passer123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8084

